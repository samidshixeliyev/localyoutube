apiVersion: 1

providers:
  - name: LocalTube
    orgId: 1
    folder: LocalTube
    folderUid: localtube
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/dashboards
